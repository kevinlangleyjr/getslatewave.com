---
name: Slatewave for Neovim
shortName: Neovim
slug: neovim
cliSlug: neovim
category: editor
tagline: A Lua colorscheme covering TreeSitter, LSP semantic tokens, and a lualine theme.
summary: >-
  A Neovim colorscheme built as a Lua plugin — TreeSitter `@*` groups,
  LSP `@lsp.*` semantic tokens, and a matching lualine theme. Syntax
  mapping mirrors the VSCode port so the same code reads the same way
  in both editors.
status: beta
order: 18
accent: '#38bdf8'
repo:
  owner: kevinlangleyjr
  name: neovim-slatewave
  url: https://github.com/kevinlangleyjr/neovim-slatewave
  defaultBranch: main
preview:
  kind: code
  language: typescript
  sample: |
    // slatewave/palette.ts
    export const accent = '#5eead4';

    export interface Theme {
      name: string;
      surface: 'editor' | 'terminal' | 'notes';
    }

    export function apply<T extends Theme>(theme: T): string {
      return `${theme.name} on ${theme.surface} (${accent})`;
    }

    const theme: Theme = {
      name: 'Slatewave',
      surface: 'editor',
    };

    console.log(apply(theme));
install:
  - label: lazy.nvim
    description: Drop the plugin spec into your lazy.nvim config. Requires Neovim 0.9+ with termguicolors enabled.
    command: |-
      return {
        "kevinlangleyjr/neovim-slatewave",
        name = "slatewave",
        priority = 1000,
        lazy = false,
        opts = {},
        config = function(_, opts)
          require("slatewave").setup(opts)
          vim.cmd.colorscheme("slatewave")
        end,
      }
    steps:
      - 'Drop the file under `~/.config/nvim/lua/plugins/` — lazy.nvim auto-imports it'
      - 'Run `:Lazy sync` and restart Neovim'
  - label: packer.nvim
    description: Add the use() call to your packer plugin list.
    command: |-
      use({
        "kevinlangleyjr/neovim-slatewave",
        as = "slatewave",
        config = function()
          require("slatewave").setup({})
          vim.cmd.colorscheme("slatewave")
        end,
      })
    steps:
      - 'Run `:PackerSync` to install'
  - label: vim-plug
    description: vim-plug has no config callback — call setup() yourself after :PlugInstall.
    command: "Plug 'kevinlangleyjr/neovim-slatewave', { 'as': 'slatewave' }"
    steps:
      - 'Add the Plug line to your init.vim / .vimrc'
      - 'Run `:PlugInstall`'
      - 'In init.lua, after the plug list — `require("slatewave").setup({}); vim.cmd.colorscheme("slatewave")`'
tags: [editor, neovim, vim]
related: [helix, vscode, sublime-text]
---

Slatewave for Neovim is a proper Lua plugin, not a dropped-in colors
file — palette, editor groups, syntax groups, TreeSitter captures, and
LSP semantic tokens each live in their own module under
`lua/slatewave/`, with a single `setup()` entry point for overrides.

Covers core UI (Normal, Pmenu, StatusLine, Visual, Diff, Diagnostics,
LSP references, spell), the full `:syntax` group set, TreeSitter `@*`
captures, LSP `@lsp.*` semantic tokens, and a matching lualine theme.
Terminal ANSI palette mirrors the VSCode theme's block so embedded
terminals stay on palette.

## Verify

After install, switch the theme at runtime — strings should be teal,
keywords sky, constants rose:

```vim
:colorscheme slatewave
```

## Configure

`require("slatewave").setup({...})` accepts these options (defaults
shown). Pass any of them in before the `:colorscheme` call:

```lua
require("slatewave").setup({
  transparent     = false,  -- set Normal bg to NONE so the terminal shows through
  italic_comments = true,
  italic_keywords = true,   -- applies to storage keywords and self / this / super
  dim_inactive    = false,
  terminal_colors = true,   -- sets g:terminal_color_0..15 to the Slatewave ANSI palette
})
```
